# IMPLEMENT Prompt Template

**Goal:** Implement the next stage as per `/docs/stages/stage-XX-*.md`.  
**Context:** Use `stages/stage-(n-1)` as base.

### Tasks
- Follow PRD and stage goals.
- Keep HTML standalone and browser-runnable.
- Maintain same coding conventions (Canvas + JS only).

### Output
- A new `/stages/stage-XX-*/index.html` file.
